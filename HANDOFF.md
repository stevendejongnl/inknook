# Handoff — inknook (E-Paper Dashboard)

> Last updated: 2026-03-13 by Claude

## Current Task
E-paper dashboard improvements: status bar clock repositioning, root Makefile with dynamic sub-delegation, git secret scanning hooks, and a context-aware motivational quote strip (Dutch/English, holiday/weather-aware).

## Progress This Session
- Moved "Updated HH:MM" clock to sit next to WiFi label in ESPHome status bar; added "Next HH:MM" right-aligned
- Fixed SNTP race (`--:--` on first render) with `wait_until` condition before render
- Created dynamic root `Makefile` that auto-delegates to `esphome/` and `backend/` sub-Makefiles via `%` catch-all
- Added `backend/Makefile` with `##` inline comments and `validate` target
- Added `.githooks/pre-commit` and `.githooks/pre-push` using `gitleaks` for secret scanning
- Added `.gitleaks.toml` with allowlists for ESPHome `!secret` refs, `.env.example`, README files
- Implemented `backend/src/services/quote.py` — context-aware quote engine (holiday, weather, weekday)
- Added Dutch (`quotes/nl.py`) and English (`quotes/en.py`) locale modules with full holiday coverage
- Wired quote into `renderer.py` (38px strip at bottom of Today column) and `display.py` router
- Added `quote_language` setting to `config.py` (default `"en"`)
- Applied `QUOTE_LANGUAGE=nl` to live Kubernetes cluster via ConfigMap patch
- Fixed Node.js 20 CI deprecation warnings with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`
- Fixed Keel not auto-deploying: root cause was OCI image index format — added `provenance: false` to `docker/build-push-action`

## Next Steps
1. Verify Keel auto-deploys on the next push (the `provenance: false` fix in `dc73b79` needs a new image push to confirm the pipeline end-to-end)
2. Add tests for `backend/src/services/quote.py`, `quotes/en.py`, `quotes/nl.py` (currently no coverage — CI threshold is 65%)
3. Flash ESPHome firmware with status bar changes (`make flash-usb` — device deep-sleeps so USB cable required)

## Key Decisions
- Used `provenance: false` in CI (not a registry workaround) — Keel's manifest poller doesn't send `Accept: application/vnd.oci.image.index.v1+json`, so GHCR rejects OCI index format silently; disabling provenance keeps Docker manifest v2 format
- Used `importlib.import_module` for locale dispatch in quote engine — adding a new language is just dropping a file in `quotes/`; no if/elif chains needed
- `gitleaks` over hand-rolled regex for secret scanning — maintained tool, GPG-signed, handles entropy analysis
- Root Makefile `%` catch-all loops over SUBDIRS at runtime — no hardcoded target list, stays in sync automatically
- Quote strip is 38px at bottom of Today column with separator line; uses `FONT_TINY` (16px) and `_wrap_text()`

## Blockers / Open Questions
- Keel auto-deploy not yet confirmed end-to-end after `provenance: false` fix (last manual verify was before the fix)
- ESPHome status bar changes not yet flashed to device (requires USB cable, device deep-sleeps)

## Git State
```
Branch: main
Status: clean (0 uncommitted changes)
Latest commit: dc73b79 — fix(ci): disable provenance to fix Keel manifest polling
Unpushed commits: 0
```

---
*Generated by /handoff skill — readable by humans and Claude alike.*
